pipeline {
  agent {
    label 'master'
  }
  stages {
    stage('deploy') {
      steps {
        echo 'Start Pull And Push Images'
        sh 'echo ababwer >> new.txt; cp new.txt sec.txt'
      }
    }
  }
}