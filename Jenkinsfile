pipeline {
    agent {
    label 'master'
    }
    
    stages {
        stage('deploy') {
            steps {
                echo "Start Pull And Push Images"
                sh "id; pwd"
            }
        }
    }
}
